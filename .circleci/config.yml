version: 2.0
jobs:
  build:
    branches:
      only:
        - devel
    docker:
      - image: brantfaircloth/phyluce-py3k:0.0.2
    steps:
       - checkout
       - run: echo 'export PYTHONPATH=/root/project:$PYTHONPATH' >> $BASH_ENV 
       - run: pytest /root/project